<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper  namespace="com.maisel.springboot.dao.StudentDao">

	<select id="getAll" resultType="com.maisel.springboot.entity.Student">
		select * from student
	</select>

	<!--<insert id="add">
		insert into student values (00,#{name},#{gender})
	</insert>

	<select id="login" resultType="com.maisel.springboot.entity.Student">
		select * from student where name=#{name} and id=#{id}
	</select>-->

</mapper>